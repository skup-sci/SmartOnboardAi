# SmartOnboardAI - Environment Configuration
# Copy this file to .env and fill in your actual API keys

# Google Gemini API key
# Get your API key from: https://makersuite.google.com/app/apikey
# This is required for AI-powered personalization features
GEMINI_API_KEY=your_gemini_api_key_here

# Instructions:
# 1. Copy this file and rename it to .env
# 2. Replace 'your_gemini_api_key_here' with your actual Gemini API key
# 3. Keep your .env file private and don't commit it to version control
# 4. Restart the app after adding your API key
#
# Alternative: You can also configure your API key through the Settings screen in the app
# The app will work without an API key but will use fallback recommendations instead of AI

# Add other API keys as needed
